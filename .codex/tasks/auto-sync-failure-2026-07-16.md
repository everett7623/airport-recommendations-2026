# 自动同步故障排查（2026-07-16）

## 目标

定位 VPSKnow 已更新但本项目未自动同步的根因，实施最小修复，并验证同步、文档生成与数据校验链路。

## TODO

- [x] 建立任务记录并读取仓库规范
- [x] 核对 GitHub Actions 最近运行记录与原始日志
- [x] 核对 VPSKnow 当前源数据和本项目数据差异
- [x] 定位根因并实施最小修复
- [x] 运行同步、生成与校验
- [x] 记录根因、验证结果和后续注意事项

## 当前证据

- 本项目最新提交停留在 `2026-07-14`。
- 故障发生时自动同步工作流仅为每周一 `00:00 UTC` 执行，也支持手动触发。
- 工作流从私有 VPSKnow 仓库检出 Astro 源文件后运行 `scripts/sync-from-astro.js`。
- 2026-07-14 的手动运行使用了 `dry_run=true`，已抓到 7 月 14 日数据，但按设计跳过提交。
- 上游 2026-07-16 页面已更新；同步器原先未读取 `fullListOnlyAirports`，会漏掉完整目录补充条目。
- 已把定时频率调整为每天 01:17 UTC，并让同步器优先同步上游完整目录补充数据。

## 验证结果

- 从 VPSKnow `main` 已提交快照 `2d1ea4c` 同步，未读取上游工作区未提交内容。
- 数据版本：`2026-07-12` → `2026-07-16`。
- 唯一有效服务商：34 → 46；`directory_only`：1 → 9；`defunct`：3 → 1。
- `npm run generate` 成功。
- `npm run validate` 成功：0 errors / 0 warnings，生成文档为 up to date。
- `git diff --check` 通过。
- 干跑模式的“不提交”行为已写入 `docs/sync-setup.md`，工作流输入说明也明确标注为仅预览。

## 状态

已完成，项目改动将随本次提交推送生效，之后由新的每日定时任务持续执行。
